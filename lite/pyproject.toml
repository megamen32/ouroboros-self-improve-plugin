[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ouroboros-self-improve"
version = "0.1.0"
description = "Runtime-neutral extraction of Ouroboros' post-task self-improvement loop"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
